# 短信计费系统需求文档（Vue \+ SpringBoot2 \+ JDK8）

我已经按照你的技术栈（**前端 Vue、后端 SpringBoot2、JDK8**），结合**按应用区分、按字数计费**的核心需求，整理了完整、可直接开发的标准化需求，包含功能、规则、接口、页面、技术要求全维度内容。

## 一、系统核心定位

一套**企业级短信计费管理系统**，支持按**不同应用**隔离短信数据，基于**短信字数**自动计算费用，实现短信发送、计费、统计、管理全流程可视化。

## 二、核心业务规则（必实现，计费核心）

### 1\. 应用隔离规则

1. 系统支持创建**多个独立应用**（如：电商 APP、后台管理系统、客户服务系统）；

2. 每个应用拥有独立的**应用 ID、应用密钥**，用于接口鉴权；

3. 短信数据、计费数据、统计数据**按应用完全隔离**，应用之间无数据互通。

### 2\. 短信字数计费规则（行业通用标准）

1. **纯文本短信**：

    - 单条短信 ≤70 字：按**1 条**计费；

    - 单条短信 ＞70 字：按**67 字 / 条**拆分计费（长短信行业标准）；

    - 示例：150 字短信 = 150/67 ≈ 3 条，按 3 条计费。

2. **特殊字符规则**：

    - 中文、英文、数字、标点符号、空格、换行**均算 1 个字**；

    - emoji 表情：按**2 个字**计算（兼容主流运营商规则）。

3. **单价配置规则**：

    - 支持**按应用配置单价**（不同应用可设置不同单价，如 0\.05 元 / 条、0\.06 元 / 条）；

    - 总费用 = 计费条数 × 应用单价。

### 3\. 短信状态规则

- 待发送：已提交未发送

- 发送成功：运营商回执成功

- 发送失败：运营商回执失败

- 失败短信**不计费**。

## 三、系统功能模块需求

### 模块 1：系统管理（基础）

1. 用户登录 / 退出（管理员账号）

2. 密码修改

3. 登录日志查看

### 模块 2：应用管理（核心隔离模块）

1. 应用列表：展示所有应用（应用 ID、应用名称、密钥、状态、单价、创建时间）

2. 新增应用：自动生成唯一**应用 ID、AppKey**，手动填写应用名称、设置单价

3. 编辑应用：修改名称、单价、状态（启用 / 禁用）

4. 删除应用：仅无短信数据的应用可删除

5. 密钥重置：一键重置 AppKey（保障接口安全）

### 模块 3：短信发送管理

#### 前端页面功能

1. 单条发送：选择应用 → 填写手机号 → 输入短信内容 → 实时显示**字数、计费条数、预估费用** → 发送

2. 批量发送：选择应用 → 上传手机号文件（TXT/Excel） → 输入短信内容 → 预览计费 → 批量发送

3. 字数实时计算：输入内容时，前端实时计算字数、拆分条数、预估费用

4. 发送记录：展示所有发送记录，支持筛选

#### 后端接口功能

1. 提供**HTTP 接口**供第三方调用发送短信（鉴权：应用 ID\+AppKey）

2. 自动校验字数、计算计费条数、计算费用

3. 仅发送成功的短信计入计费统计

### 模块 4：短信计费记录

1. 列表展示：应用名称、手机号、短信内容、字数、计费条数、单价、总费用、发送状态、时间

2. 筛选功能：按应用、手机号、发送时间、发送状态筛选

3. 导出功能：支持导出 Excel 账单

4. 详情查看：查看完整短信内容和计费明细

### 模块 5：计费统计报表

1. 按应用统计：总发送量、成功量、失败量、总计费条数、总费用

2. 按时间统计：日 / 月 / 年统计（折线图展示趋势）

3. 统计筛选：按应用、时间范围筛选

4. 数据汇总：顶部展示总费用、总条数汇总数据

## 四、前端（Vue）需求

### 1\. 技术栈要求

- 框架：Vue2 / Vue3（推荐 Vue2，兼容性更好）

- UI 组件库：Element UI

- 网络请求：Axios

- 工具：ECharts（报表）、Xlsx（文件导出 / 上传）

### 2\. 核心页面

1. 登录页

2. 应用管理页（增删改查）

3. 短信发送页（单条 \+ 批量）

4. 短信记录页

5. 计费报表页

6. 个人中心页

### 3\. 前端核心能力

1. **实时字数计算**：输入内容自动计算字数、计费条数、预估费用

2. 表单校验：手机号格式、内容非空校验

3. 文件上传：批量手机号导入

4. 数据导出：Excel 导出账单

5. 权限控制：未登录自动跳转登录页

## 五、后端（SpringBoot2 \+ JDK8）需求

### 1\. 技术栈要求

- 框架：SpringBoot 2\.6\.x \~ 2\.7\.x

- JDK：1\.8

- 数据库：MySQL 5\.7\+

- 持久层：MyBatis/MyBatis\-Plus

- 工具：Hutool（工具类）、POI（Excel）

### 2\. 核心接口

|接口模块|接口名称|请求方式|鉴权|
|---|---|---|---|
|登录|管理员登录|POST|否|
|应用管理|应用增删改查|GET/POST|是|
|短信发送|单条发送|POST|应用鉴权|
|短信发送|批量发送|POST|应用鉴权|
|计费记录|记录列表 / 导出|GET|是|
|统计报表|数据统计|GET|是|

### 3\. 后端核心能力

1. **应用鉴权**：验证应用 ID\+AppKey，非法请求直接拒绝

2. **字数计算工具类**：统一计算字数、计费条数

3. **计费计算**：自动计算单条 / 批量费用

4. **事务控制**：发送短信 \+ 记录计费保证原子性

5. **异常处理**：全局统一异常返回

6. **分页查询**：所有列表接口支持分页

## 六、数据库设计（核心表）

### 1\. 用户表（sys\_user）

管理员账号信息

### 2\. 应用表（sms\_application）

应用 ID、应用名称、AppKey、单价、状态、创建时间

### 3\. 短信记录表（sms\_record）

应用 ID、手机号、内容、字数、计费条数、单价、总费用、发送状态、创建时间

### 4\. 登录日志表（sys\_login\_log）

登录记录

## 七、非功能需求

1. 安全性：接口鉴权、密钥加密存储、SQL 防注入

2. 性能：批量发送支持 1000 条以内无卡顿

3. 兼容性：JDK8、MySQL5\.7、主流浏览器

4. 可扩展性：支持后续对接真实短信网关、增加短信模板

## 八、开发优先级

1. 核心：应用管理 \+ 短信字数计费 \+ 短信发送 / 记录

2. 次要：统计报表、批量发送、导出

3. 优化：前端实时计算、接口鉴权

---

### 总结

1. 系统核心：**应用隔离 \+ 按字数自动计费**，完全贴合你的业务需求；

2. 技术栈严格匹配：**Vue \+ SpringBoot2 \+ JDK8**，无额外学习成本；

3. 需求覆盖：功能、规则、前后端、数据库全流程，可直接用于开发和评审。

我可以基于这个需求，继续为你生成：**数据库建表 SQL、后端接口文档、前端页面原型、核心工具类代码**，你需要哪一部分？

> （注：文档部分内容可能由 AI 生成）
