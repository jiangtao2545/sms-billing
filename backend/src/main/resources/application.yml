server:
  port: 8080

spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:kingbase8://10.154.33.107:54321/gtcap?useUnicode=true&characterEncoding=utf8
    username: gtcap
    password: K1ngb@se!#23
    driver-class-name: com.kingbase8.Driver
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      filters: slf4j
      filter:
        stat:
          slow-sql-millis: 2000
          log-slow-sql: true
        wall:
          enabled: true
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        login-username: admin
        login-password: admin123
        reset-enable: false
      web-stat-filter:
        enabled: true
        url-pattern: /*
        exclusions: '*.js,*.gif,*.jpg,*.png,*.css,*.ico,/druid/*'
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: Asia/Shanghai

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: sms-billing-secret-key-2024
  expiration: 86400

sms:
  gateway:
    base-url: https://sms-hy.jdcloud.com/HttpSmsMt
    name: tyjthy
    password: d41d8cd98f00b204e9800998ecf8417e
    subid: 1
    callback-host: http://your-host:8080
